{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-sm">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-lg btn-primary btn-block" type="submit">
                {{ 'action_search'|trans }}
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
