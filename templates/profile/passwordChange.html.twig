{% extends 'base.html.twig' %}

{% block title %}{{ 'profile_title_password_change'|trans }}{% endblock %}

{% block headerContent %}
    <div id="profile-password-change">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">{{ 'action_submit'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
